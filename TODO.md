- Try and remove TERM= requirement for Docker and SSH
    - With SSH, perhaps create wrapper script that auto sets it
- Package the following
    - opengrep
- Nvim prettier formatter for GraphQL
- Generally try and refactor sandboxing to have "backends"
    - Try and migrate things to Bubblewrap and standardise bwrap sandboxing
    - With docker mounts, if the dir doesn't exist docker will create it. But this means its created as root with bad dir ownership. Eg. jwt_tool config
        - Make a more standardised way of doing this
