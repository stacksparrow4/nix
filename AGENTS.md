This is a NixOS repo using flakes.

## Structure

- **`hosts/`**: Per-host configurations (physical machines, VMs)
- **`vms/`**: VM definitions and templates
- **`pkgs/`**: Custom packages
- **`common/`**: Shared Nix modules
